export const Headers = require('./header.vue').default;

export const Home = require('./home.vue').default;

export const About = require('./about.vue').default;

export const Contact = require('./contact.vue').default;